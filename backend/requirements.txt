# Exact pins, not floors. `>=` let the image build pick up pandas 3 while dev
# ran pandas 2, and pandas 3 returns a SQL NULL string as NaN instead of None:
# a production-only 500 that no local test could reproduce. Bump these
# deliberately, and test under the new versions before the image does.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pandas==3.0.6
numpy==2.5.3
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
google-cloud-bigquery==3.45.2
# Optional to import, not optional in practice: without it the BigQuery client
# silently falls back to the REST iterator, which turned a ~2 minute Arrow pull
# of 3M rows into 1,603s on a production cold start. Imported lazily behind
# except ImportError in bigquery_service.py, so nothing fails loudly.
google-cloud-bigquery-storage==2.41.0
db-dtypes==1.7.1
httpx==0.28.1
duckdb==1.5.5
pyarrow==25.0.1
# backend/services/excel_report.py imports this at module scope, and
# routers/commercial.py imports that, so a missing openpyxl is not a broken
# export button -- it is main.py failing to import and the pod never starting.
openpyxl==3.1.5
